import { styled } from "styled-components";
import { FlexContainer } from "styles/layouts";

export const PaginationContainer = styled(FlexContainer)`
  bottom: 0;
  width: 100%;
  margin-top: 2rem;
`;
